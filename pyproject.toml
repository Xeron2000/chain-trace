[project]
name = "chain-trace-tools"
version = "0.2.0"
description = "Zero-key crypto forensics for ETH / Base / BSC / Solana"
requires-python = ">=3.10"
dependencies = [
  "cloudscraper>=1.2.71",
  "requests>=2.31.0",
  "scikit-learn>=1.3.0",
  "numpy>=1.24.0",
]

[tool.uv]
package = false

[dependency-groups]
dev = ["pytest>=8.0"]
